# EO-DB Duplicate and Bypass Report

The current registry reports 0 duplicate production source-target pairs and 0 registered bypass bridges.

EO-DG route auditing still reports read surface registration gaps as blocking architecture findings. These are tracked as read-only drift rather than production bypasses.

