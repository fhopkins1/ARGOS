# EO-DI Test Execution Engine

EO-DI provides `run_test_command()` to capture command, commit, environment, stdout, stderr, exit code, duration, pass/fail/error signals, timeout, incompleteness, and hashes.

It does not suppress failing output.

