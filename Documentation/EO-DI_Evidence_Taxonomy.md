# EO-DI Evidence Taxonomy

EO-DI evidence classes include source inspection, static call graph, bridge graph, test result, runtime trace, transaction trace, recovery trace, read-integrity digest, synthetic-truth scan, fault-campaign result, endurance time series, and environment limitation.

Every evidence item is commit-linked and hashed where the raw artifact exists.

