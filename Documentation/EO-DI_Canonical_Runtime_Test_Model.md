# EO-DI Canonical Runtime Test Model

EO-DI distinguishes direct component tests from tests that reach `CanonicalEnterpriseRuntime` or the canonical runtime provider.

Compatibility runtime tests remain useful but are not treated as production canonical runtime proof.

