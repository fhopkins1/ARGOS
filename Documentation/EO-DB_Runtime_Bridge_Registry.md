# EO-DB Runtime Bridge Registry

The canonical bridge registry is implemented in `src/argos/control_panel/runtime_bridge_certification.py` as `RuntimeBridgeRegistry` and `required_runtime_bridge_matrix()`.

The registry currently declares 30 required constitutional bridges. The generated machine-readable registry is `Documentation/EO-DB_Evidence/EO-DB_bridge_graph.json`.

Current state totals: 4 certified production, 11 conditionally production, 14 partial, 0 missing, 0 duplicate, 0 bypass, and 1 replay-only bridge.

