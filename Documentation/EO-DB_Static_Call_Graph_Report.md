# EO-DB Static Call Graph Report

EO-DB includes repository-compatible AST analysis through `static_call_graph()`. Static analysis records imports and call edges for discovery only; it does not certify production reachability by itself.

The generated CSV is `Documentation/EO-DB_Evidence/EO-DB_call_edges.csv`. Current static edge total: 26,809.

