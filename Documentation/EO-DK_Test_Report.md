# EO-DK Test Report

Targeted command:

`py -3 -m unittest Tests.test_eodk_bridge_fabric`

The suite verifies duplicate bridge rejection, ownership transfer, information-only delivery, artifact hash rejection, replay-domain rejection, idempotent duplicate success, disabled bridge rejection, and canonical runtime bridge trace emission.
