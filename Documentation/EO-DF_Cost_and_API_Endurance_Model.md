# EO-DF Cost and API Endurance Model

EO-DF tracks API calls, API failures, retries, total cost, idle cost, workflow delta use, freshness failures, cache hits, and cache misses.

Idle and read-only campaigns require zero API calls and zero cost unless explicitly justified by a versioned pass-criteria change.

