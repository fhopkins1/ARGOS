# EO-DI CI Enforcement

EO-DI adds focused tests in `Tests/test_eodi_test_evidence_office.py`.

CI should fail when blocking requirements lack tests or evidence, critical bridges lack canonical tests, synthetic findings lack regression tests, live becomes reachable, or full-suite failures are hidden.

