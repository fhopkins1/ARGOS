# EO-DH Unknown and Unavailable State Model

`unavailable_state()` creates explicit unavailable records that cannot default to zero or empty and cannot satisfy authoritative requirements.

Downstream behavior must fail closed for paper decision and paper ledger consumers.

